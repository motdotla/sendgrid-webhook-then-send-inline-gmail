{
  "name": "sendgrid-webhook-then-send-inline-gmail",
  "version": "0.0.1",
  "description": "This little app is attempting to demonstrate how to send an email, then a robot (SendGrid) catches the email, and sends a reply back. That reply should be inline.",
  "main": "app.js",
  "dependencies": {
    "sendgrid": "~1.0.3",
    "hapi": "~5.0.0",
    "dotenv": "~0.2.8"
  }
}
